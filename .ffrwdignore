# Build output. The files the lib SQL names are pulled back in by the
# manifest's closure; the rest of the directory stays out of the archive.
target/

# The wit the module's build.rs copies in, which is the sidecar's own. The
# module's own world lives in wit-world/ and ships.
wit/

# Local development state, not package content.
ffrwd.lock
